const configuration = {
  firstTest: true,
  allConfigurationTested: true
};

const firebase = {
  analytics: {
    logScreenView: {
      causeError: false,
      errorMessage: 'by default',
    },
  },
};

export default {
  firebase,
  configuration
};
