import {OperationName, VirtualOperationName} from '@hiveio/dhive';

const cases: {
  opKeyNameAndFunction: OperationName | VirtualOperationName;
}[] = [
  {
    opKeyNameAndFunction: 'transfer',
  },
];
export default {cases};
