import { HAS_AuthPayload } from "utils/hiveAuthenticationService/payloads.types";

export default {
    payload: {
        justUUID: {
            uuid: 'UUID-001',
        } as HAS_AuthPayload,
    },
};