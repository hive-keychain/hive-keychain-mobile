import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Swap = () => {
  return (
    <View>
      <Text>Swap TODO</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Swap;
