import * as Colors from './colors';
import * as Spacing from './spacing';
import * as Text from './text';

export {Colors, Spacing, Text};

// More styling files can be added, i.e. containers.ts file with different flex options, etc.
// Also potentially a function linking to some image assets, according to the theme
