import React, {useState} from 'react';
import {Text, View, StyleSheet} from 'react-native';

const Tokens = ({components, menu, toUpperCase, style}) => {
  return (
    <View>
      <Text>Tokens</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Tokens;
